.list-item.container-fluid{ id: "rec-#{rec.id}" }
  %tr
    %td.rec-url-col
      =rec.url
    %td.rec-chat-col
      =rec.chat
    %td.rec-slide-col
      =rec.slide
    %td.rec-name-col
      =rec.name
